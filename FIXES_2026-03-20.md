# 修复总结 - 2026-03-20

## 已完成的修复

### 1. 按项目导出报告功能修复
**问题**: 按项目导出时只返回第一个项目的报告，多项目时会丢失数据

**修复**: 
- 文件: `index/controllers/ProductsControllers.php` 
- 位置: `download_by_project_index()` 方法 (line ~1236-1268)
- 修改内容: 添加了多文件ZIP打包支持，与单个漏洞导出逻辑保持一致
  - 单个项目: 直接下载 `.docx` 文件
  - 多个项目: 打包成 `.zip` 文件下载

**测试步骤**:
1. 登录系统 (`admin / 123456.`)
2. 导航到"漏洞列表"
3. 在项目下拉框选择"贵州高速渗透测试"项目
4. 点击"按项目导出报告"按钮
5. 选择导出模板
6. 验证下载的文件是有效的 Word 文档，包含所有漏洞信息

**预期结果**:
- 下载成功，文件名如: `贵州高速渗透测试安全测试报告2026-03-20.docx`
- 打开 Word 文档，应包含:
  - 文档信息表
  - 工作概述
  - 漏洞详情列表（所有漏洞）
  - 每个漏洞的截图（清晰、完整）

---

### 2. 剪贴板图片粘贴功能优化
**问题A**: 粘贴截图时，图片被内嵌为 base64，导出报告时图片丢失

**修复**:
- 文件: 
  - `index/view/products/add_index.tpl`
  - `index/view/products/edit_index.tpl`  
  - `index/view/products/repair_index.tpl`
- 修改内容: 
  - 在 `onPaste` 回调中增加剪贴板图片检测
  - 将剪贴板图片上传到服务器 (`public_deup_img`)
  - 插入服务器 URL 引用，而非 base64 内嵌
  - 移除固定宽度样式，保持原图清晰度

**问题B**: 粘贴图片时出现两张图片（重复插入）

**修复**:
- 在检测到图片时立即阻止事件传播:
  ```javascript
  ne.preventDefault ? ne.preventDefault() : (ne.returnValue = false);
  ne.stopPropagation && ne.stopPropagation();
  ne.stopImmediatePropagation && ne.stopImmediatePropagation();
  ```
- 防止 Summernote 的默认粘贴处理重复插入图片

**测试步骤**:
1. 登录系统
2. 导航到"添加漏洞"页面
3. 使用截图工具（如 Snipping Tool）截取一张图片
4. 在"漏洞内容"编辑器中按 `Ctrl+V` 粘贴
5. 观察编辑器中的图片数量

**预期结果**:
- ✅ 只显示 **1张** 图片（不是2张）
- ✅ 图片显示为服务器链接（URL pattern: `/index.php?***m=Public&a=enup_img&id=***`）
- ✅ 不是 base64 格式（不以 `data:image/png;base64,` 开头）
- ✅ 图片以原始尺寸显示（无强制缩放）

---

### 3. 图片上传清晰度优化
**修改**: 移除了插入图片时的 `width:50%` 样式限制

**原因**: 
- CSS 样式 `width:50%` 只影响浏览器显示
- 但防止用户误以为图片已被压缩
- 导出 Word 时使用的是服务器原始文件，不受 CSS 影响

**验证点**:
- 服务器存储图片: `public/auto/<md5hash>` (RC4+gzcompress加密)
- 存储的是原始图片数据，无压缩、无缩放
- `PublicControllers::deup_img()` 方法不做任何图片处理
- Python 导出服务读取原始文件并嵌入 Word

---

## 图片上传功能限制

### Upload 类配置 (`classes/upload.php`)
```php
protected $maxSize = 5 * 1024 * 1024;  // 最大5MB
protected $allowSuffix = ['jpg', 'jpeg', 'gif', 'png'];
protected $allowMime = ['image/jpg', 'image/jpeg', 'image/gif', 'image/wbmp', 'image/png'];
```

### 常见错误信息
| 错误代码 | 错误信息 | 原因 |
|---------|---------|------|
| -1 | 文件路径没有设置 | 服务器配置问题 |
| -2 | 文件不是目录或者不可写 | 权限问题 |
| -3 | 文件超过指定大小 | 文件大于5MB |
| -4 | mime类型不符合 | 非图片文件或格式不支持 |
| -5 | 文件后缀不符合 | 只支持 jpg/jpeg/gif/png |
| -6 | 不是上传文件 | 非法上传请求 |
| -7 | 移动失败 | 磁盘空间或权限问题 |

---

## 如果遇到"无法正常上传原始图片"问题

### 诊断步骤:

1. **检查文件大小**
   ```bash
   # 如果图片大于5MB，需要修改限制
   # 编辑: classes/upload.php
   # 修改: protected $maxSize = 10 * 1024 * 1024; // 改为10MB
   ```

2. **检查浏览器控制台**
   - 按 F12 打开开发者工具
   - 切换到 Network (网络) 标签
   - 尝试上传图片
   - 查看 `public_deup_img` 请求的响应:
     - 成功: `{"status": 1, "msg": "文件上传成功", "data": "..."}`
     - 失败: `{"status": 0, "msg": "具体错误信息"}`

3. **检查 PHP 配置**
   ```php
   // 查看 php.ini
   upload_max_filesize = 10M  // 上传文件最大大小
   post_max_size = 10M        // POST数据最大大小
   memory_limit = 128M        // PHP内存限制
   ```

4. **检查目录权限**
   ```powershell
   # 确保 public/auto/ 目录可写
   Test-Path "C:\Users\admin\Documents\company\CompanyToolDevelopment\BugRepoter_0x727\public\auto"
   ```

5. **查看错误日志**
   - 数据库日志表: `domain_log`
   - 查询最近的上传错误:
     ```sql
     SELECT * FROM domain_log 
     WHERE title LIKE '%上传文件%' OR title LIKE '%异常%' 
     ORDER BY create_time DESC 
     LIMIT 10;
     ```

---

## 完整测试流程

### 测试1: 工具栏上传图片
1. 登录 → 添加漏洞
2. 点击编辑器工具栏的"图片"按钮
3. 选择一张本地图片（建议 < 5MB）
4. 观察是否成功插入
5. 检查图片 URL 是否为服务器链接

### 测试2: 剪贴板粘贴图片
1. 使用截图工具截取屏幕
2. 在编辑器中按 Ctrl+V
3. 观察是否只出现1张图片
4. 检查图片 URL 格式

### 测试3: 保存并导出
1. 填写完整的漏洞信息（标题、分类、等级、漏洞内容等）
2. 保存漏洞
3. 返回漏洞列表
4. 点击单个漏洞的"导出漏洞报告"
5. 打开下载的 Word 文档
6. 验证图片清晰度和完整性

### 测试4: 按项目导出
1. 确保项目下有多个漏洞（至少2个）
2. 在漏洞列表选择项目
3. 点击"按项目导出报告"
4. 验证下载的文档包含所有漏洞

---

## 后续改进建议

1. **文件名去重优化**
   - 当前 `deup_img()` 使用 `md5(time().$path.time().code())` 生成文件名
   - 同一秒内上传多张图可能冲突
   - 建议改为: `md5(uniqid(mt_rand(), true).code())`

2. **文件大小限制提示**
   - 前端增加文件大小检测
   - 超过5MB时给出明确提示
   - 建议用户压缩图片或调整限制

3. **图片预览功能**
   - 粘贴/上传后显示加载状态
   - 上传成功后显示缩略图预览

4. **错误提示优化**
   - 当前错误信息只显示在 layer.msg
   - 建议增加更详细的错误信息和解决方案

---

## 技术细节

### 图片存储流程
```
用户上传
  ↓
Upload::uploadFile() 验证 (大小/类型/后缀)
  ↓
移动到 public/auto/ 临时文件
  ↓
binary_encode() RC4加密 + gzcompress压缩
  ↓
保存为 public/auto/<md5hash> (无扩展名)
  ↓
删除临时文件
  ↓
返回 URL: /index.php?...m=Public&a=enup_img&id=<md5hash>
```

### 图片读取流程
```
浏览器请求 /index.php?...m=Public&a=enup_img&id=<md5hash>
  ↓
PublicControllers::enup_img()
  ↓
file_get_contents('public/auto/<md5hash>')
  ↓
binary_decode() 解密解压
  ↓
echo 输出原始图片数据
  ↓
浏览器显示
```

### 导出 Word 流程
```
PHP 提取漏洞内容中的图片 URL
  ↓
正则匹配: m=Public&a=enup_img&id=<hash>
  ↓
读取 public/auto/<hash> 并解密
  ↓
保存为 python_web/tmp/<hash>.png 临时文件
  ↓
JSON 数据发送给 Python Socket 服务
  ↓
Python 读取临时文件, 嵌入 docx
  ↓
生成最终报告并返回
  ↓
PHP 下载文件, 清理临时文件
```

---

## 修改的文件清单

1. `index/controllers/ProductsControllers.php` - 按项目导出多文件支持
2. `index/view/products/add_index.tpl` - 剪贴板图片上传 + 防重复
3. `index/view/products/edit_index.tpl` - 剪贴板图片上传 + 防重复
4. `index/view/products/repair_index.tpl` - 剪贴板图片上传 + 防重复

---

## Git 提交记录

```bash
git add index/controllers/ProductsControllers.php
git add index/view/products/add_index.tpl
git add index/view/products/edit_index.tpl
git add index/view/products/repair_index.tpl
git commit -m "修复按项目导出报告功能和剪贴板图片粘贴问题

- 按项目导出支持多文件ZIP打包
- 剪贴板图片自动上传服务器（保持原图清晰度）
- 修复粘贴图片时出现两张图的问题
- 移除图片插入时的固定宽度限制"
```
